# Optional: API base URL when frontend is deployed separately (e.g. Vercel)
# Leave empty or omit when backend serves the frontend (same origin).
# Example for Vercel frontend + Railway backend:
# VITE_API_URL=https://your-backend.up.railway.app/api
